---
import Base from '../layouts/Base.astro';
---

<Base>
  <section class="hero">
    <h1 class="hero-headline">I write essays, build products, and review films with my partner.</h1>
    <p class="hero-sub">I'm interested in how things get made — and why most of them don't.</p>
  </section>

  <section class="work-section">
    <span class="label">Projects</span>
    <h2 class="work-title">Neon Puck</h2>
    <p class="work-desc">2-player air hockey with online multiplayer. Built with Canvas + Socket.io.</p>
    <a href="/projects" class="work-link">View all projects &rarr;</a>
  </section>

  <section class="work-section">
    <span class="label">Writing</span>
    <h2 class="work-title">Essays & Newsletters</h2>
    <p class="work-desc">Personal essays, film reviews, and thoughts on building.</p>
    <a href="/writing" class="work-link">Read &rarr;</a>
  </section>

  <section class="work-section">
    <span class="label">Film & Video</span>
    <h2 class="work-title">Film & Video</h2>
    <p class="work-desc">IMDB credits, reviews with Basic Acts, and video work.</p>
    <a href="/film" class="work-link">Explore &rarr;</a>
  </section>

  <section class="work-section">
    <span class="label">Photography</span>
    <h2 class="work-title">Photo Gallery</h2>
    <p class="work-desc">Selected photographs.</p>
    <a href="/photography" class="work-link">View &rarr;</a>
  </section>

  <section class="work-section work-section--muted">
    <span class="label">Stories</span>
    <h2 class="work-title">Short Fiction</h2>
    <p class="work-desc">Denton TX 1995 and other works. Coming soon.</p>
  </section>

  <section class="now-section">
    <span class="label">Now</span>
    <p class="now-text">Working on a short story collection. Writing the Millennium Wave series. Trying to get outside more.</p>
  </section>
</Base>

<style>
  .hero {
    padding-bottom: var(--section-space);
  }

  .hero-headline {
    font-size: var(--text-huge);
    font-weight: 600;
    letter-spacing: -0.03em;
    line-height: 1.05;
    max-width: 18ch;
    margin-bottom: 24px;
  }

  .hero-sub {
    font-size: var(--text-large);
    color: var(--fg-50);
    line-height: 1.4;
    max-width: 36ch;
  }

  /* ===== Work Sections ===== */
  .work-section {
    padding-top: var(--section-space);
    border-top: 1px solid var(--fg-10);
  }

  .work-section--muted {
    opacity: 0.4;
  }

  .label {
    display: block;
    font-size: var(--text-xs);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--fg-33);
    margin-bottom: 16px;
  }

  .work-title {
    font-size: var(--text-large);
    font-weight: 600;
    letter-spacing: -0.02em;
    margin-bottom: 8px;
  }

  .work-desc {
    font-size: var(--text-body);
    color: var(--fg-50);
    line-height: 1.6;
    margin-bottom: 20px;
  }

  .work-link {
    font-size: var(--text-small);
    color: var(--fg-50);
    border-bottom: 1px solid var(--fg-25);
    padding-bottom: 1px;
    transition: color 0.15s ease, border-color 0.15s ease;
  }

  .work-link:hover {
    color: var(--fg);
    border-color: var(--fg);
  }

  /* ===== Now ===== */
  .now-section {
    padding-top: var(--section-space);
    border-top: 1px solid var(--fg-10);
  }

  .now-text {
    font-size: var(--text-body);
    color: var(--fg-50);
    line-height: 1.6;
  }
</style>

---
import Base from '../layouts/Base.astro';
---

<Base>
  <section class="hero">
    <h1 class="hero-headline">I write essays and build products.</h1>
    <p class="hero-sub">I'm interested in how things get made — and why most of them don't.</p>
  </section>

  <section class="work-section">
    <span class="label">Projects</span>
    <h2 class="work-title">Neon Puck</h2>
    <p class="work-desc">2-player air hockey with online multiplayer. Built with Canvas + Socket.io.</p>
    <a href="/projects" class="work-link">View all projects &rarr;</a>
  </section>

  <section class="work-section">
    <span class="label">Essays</span>
    <h2 class="work-title">Writing on Substack</h2>
    <p class="work-desc">Personal essays and thoughts on building.</p>
    <a href="https://toddperumal.substack.com" target="_blank" rel="noopener" class="work-link">Read on Substack &rarr;</a>
  </section>

  <section class="work-section">
    <span class="label">Career</span>
    <h2 class="work-title">Background & Experience</h2>
    <p class="work-desc">Where I've worked and what I've learned along the way.</p>
    <a href="/career" class="work-link">View career &rarr;</a>
  </section>
</Base>

<style>
  .hero {
    padding-bottom: var(--section-space);
  }

  .hero-headline {
    font-size: var(--text-huge);
    font-weight: 600;
    letter-spacing: -0.03em;
    line-height: 1.05;
    max-width: 18ch;
    margin-bottom: 24px;
  }

  .hero-sub {
    font-size: var(--text-large);
    color: var(--fg-50);
    line-height: 1.4;
    max-width: 36ch;
  }

  /* ===== Work Sections ===== */
  .work-section {
    padding-top: var(--section-space);
    border-top: 1px solid var(--fg-10);
  }

  .label {
    display: block;
    font-size: var(--text-xs);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--fg-33);
    margin-bottom: 16px;
  }

  .work-title {
    font-size: var(--text-large);
    font-weight: 600;
    letter-spacing: -0.02em;
    margin-bottom: 8px;
  }

  .work-desc {
    font-size: var(--text-body);
    color: var(--fg-50);
    line-height: 1.6;
    margin-bottom: 20px;
  }

  .work-link {
    font-size: var(--text-small);
    color: var(--fg-50);
    border-bottom: 1px solid var(--fg-25);
    padding-bottom: 1px;
    transition: color 0.15s ease, border-color 0.15s ease;
  }

  .work-link:hover {
    color: var(--fg);
    border-color: var(--fg);
  }
</style>
